{
  "name": "motion-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based motion design studio — pick a template, drop an image, export a looping video. Fully client-side.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler@latest pages deploy dist --project-name motion-studio"
  },
  "dependencies": {
    "@fontsource/cairo": "^5.3.0",
    "@fontsource/ibm-plex-sans-arabic": "^5.3.0",
    "@fontsource/tajawal": "^5.3.0",
    "idb-keyval": "^6.2.1",
    "mp4-muxer": "^5.1.5",
    "webm-muxer": "^5.0.3"
  },
  "devDependencies": {
    "linkedom": "^0.18.13",
    "vite": "^5.4.10"
  }
}
